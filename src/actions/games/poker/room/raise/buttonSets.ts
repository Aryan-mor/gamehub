/**
 * Button sets for raise action - different raise amount options
 */
export const raiseOptions = {
  // All raise amount options (3 buttons per row)
  all: [['raise5', 'raise10', 'raise25'], ['raise50', 'raise100']],
  
  // Quick raise options (3 buttons per row)
  quick: [['raise5', 'raise10', 'raise25']],
  
  // High stakes raise options (2 buttons per row)
  high: [['raise50', 'raise100']],
  
  // Low stakes raise options (2 buttons per row)
  low: [['raise5', 'raise10']],
  
  // Medium stakes raise options (2 buttons per row)
  medium: [['raise25', 'raise50']]
}; 