/**
 * Button sets for stake selection - different stake amount options
 */
export const stakeOptions = {
  // Standard stake selection buttons (3 buttons per row)
  standard: [['stake2', 'stake5', 'stake10'], ['stake20', 'stake50']],
  
  // Low stakes options (3 buttons per row)
  low: [['stake2', 'stake5', 'stake10']],
  
  // High stakes options (2 buttons per row)
  high: [['stake20', 'stake50']],
  
  // Quick stake options (3 buttons per row)
  quick: [['stake5', 'stake10', 'stake20']],
  
  // All stake options (3 buttons per row)
  all: [['stake2', 'stake5', 'stake10'], ['stake20', 'stake50']]
}; 